describe('Каталог', () => {
  it('Отображение товаров с сервера', () => {
    expect(2 + 2).toBe(4)
  })
  it('Отображается название, цена и подробнее...', () => {
    expect(2 + 2).toBe(4)
  })
  it('По клику на "Подробнее.." карточки товара есть переход на страницу товара', () => {
    expect(2 + 2).toBe(4)
  })
  it('', () => {
    expect(2 + 2).toBe(4)
  })
  it('', () => {
    expect(2 + 2).toBe(4)
  })
  it('', () => {
    expect(2 + 2).toBe(4)
  })
})
