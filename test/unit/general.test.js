describe('Общие требования', () => {
  it('Адаптация верстки под ширину', () => {
    expect(2 + 2).toBe(4)
  })
  it('Есть ссылки на страницы и на корзину', () => {
    expect(2 + 2).toBe(4)
  })
  it('Название магазина кликабельно и ведет на главную страницу', () => {
    expect(2 + 2).toBe(4)
  })
  it('Есть гамбургер (width <= 576px)', () => {
    expect(2 + 2).toBe(4)
  })
  it('Закрытие меню гамбургера при выборе элемента (width <= 576px)', () => {
    expect(2 + 2).toBe(4)
  })
})
